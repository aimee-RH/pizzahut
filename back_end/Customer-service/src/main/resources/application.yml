server:
  port: 8086

spring:
  profiles:
    active: dev # 环境设置

  application:
      name: Customerservice


  cloud:
    nacos:
      server-addr: localhost:8848
      discovery:
        cluster-name: BJ  #集群名称

  jackson:
    time-zone: GMT+8
    date-format: yyyy-MM-dd HH:mm:ss

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/pizzahut?serverTimezone=Asia/Shanghai&useSSL=false&characterEncoding=UTF-8&allowPublicKeyRetrieval=true
    username: PizzaHutUser
    password: "f25012501"